server {
    listen 80;
    server_name localhost;
    
    # Laravel siempre expone la carpeta public, no la raíz
    root /var/www/html/public;

    add_header X-Frame-Options "SAMEORIGIN";
    add_header X-XSS-Protection "1; mode=block";
    add_header X-Content-Type-Options "nosniff";

    index index.php;

    charset utf-8;

    # Si la ruta no existe físicamente, redirige al router de Laravel
    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    error_page 404 /index.php;

    # Comunicación con el contenedor de PHP (llamado "app" en el docker-compose)
    location ~ \.php$ {
        fastcgi_pass app:9000;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
    }

    # Bloquear el acceso a archivos ocultos (como .env) por seguridad
    location ~ /\.(?!well-known).* {
        deny all;
    }
}
